# Simplest possible use case: run claude, no other setup required. Pulls
# the public Docker Hub image — nothing to build or configure first.
#
#   sbx env run examples/minimal.sbxenv.yaml

schemaVersion: "1"
agent: claude-sbx
workspace: .

kits:
  - git+https://github.com/ypxing/sbx-agent-kits.git#dir=kits/agents/claude
