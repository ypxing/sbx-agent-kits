# Bedrock patch — appended onto config/codex/config.toml's "yolo mode" base
# by apply-config.sh. Keys here must stay disjoint from the base file;
# see apply-config.sh's apply_patch() for why that's safe.

model_provider = "amazon-bedrock"

[model_providers.amazon-bedrock]
wire_api = "responses"

[model_providers.amazon-bedrock.aws]
region = "us-east-1"
